import { StoryTitleFormatterPipe } from './story-title-formatter.pipe';

describe('StoryTitleFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new StoryTitleFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
